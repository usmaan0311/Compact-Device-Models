import pandas as pd, matplotlib.pyplot as plt, glob

f=glob.glob("*.dat");

df=pd.read_csv(f[0], sep="\s+")

v, p = df.iloc[:,0], df.iloc[:,1]

plt.plot(v,p, label="Surface Potential")
plt.xlabel("Gate Bias (V)", fontsize=18)
plt.ylabel(r"$\Psi_{s}(V)$", fontsize=18)
plt.legend()
plt.show()
